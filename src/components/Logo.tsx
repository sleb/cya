import { Button, Typography } from "@mui/material";

const Logo = () => {
  return (
    <Button>
      <Typography
        fontWeight="bold"
        fontSize={24}
        letterSpacing={-1}
        color="white"
      >
        $$
      </Typography>
    </Button>
  );
};

export default Logo;
